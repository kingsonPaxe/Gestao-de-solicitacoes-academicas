services:
- type: web
  name: modulo-solicitacoes
  env: python
  buildCommand: uv sync --frozen && uv cache prune --ci
  startCommand: uv run gunicorn run:app
  envVars:
    - key: PYTHON_VERSION
      value: 3.13.5
    - key: FLASK_ENV
      value: production
